local M = {
	start = require("inlyne.commands.sub_commands.start"),
	status = require("inlyne.commands.sub_commands.status"),
	stop = require("inlyne.commands.sub_commands.stop"),
	toggle = require("inlyne.commands.sub_commands.toggle"),
}

return M
